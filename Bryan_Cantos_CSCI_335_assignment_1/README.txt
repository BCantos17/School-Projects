i) All parts were completeled

ii) If user inputs a char, the code will read it as a 0

iii) User must only type in numbers and when finish, must type in stop, must be lower case. 

	When compiling on windows, you must have the line "g++ main.cpp -o main -std=c++11" or if on Linux "g++ main.cpp -o main -std=c++0x". Must have document1.txt in directory.

iv) no input or output files.